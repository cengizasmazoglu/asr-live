# asr-live (training)

This repo contains:
- dataset prep scripts (MP3+SRT -> segments + manifest)
- Whisper fine-tune training code (later)
- experiment logging (W&B later)

Large data (audio/segments/checkpoints) is intentionally ignored.
